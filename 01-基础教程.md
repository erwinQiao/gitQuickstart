# Git 基础教程

## 目录
- [Git 是什么](#git-是什么)
- [为什么使用 Git](#为什么使用-git)
- [Git 核心概念](#git-核心概念)
- [Git 基本操作](#git-基本操作)
- [分支管理基础](#分支管理基础)
- [常见问题解答](#常见问题解答)

> 分支管理基础中包含 [Squash（压缩提交）](#squash压缩提交) 的详细讲解。

---

## Git 是什么

### 定义

Git 是一个**分布式版本控制系统**，由 Linux 之父 Linus Torvalds 于 2005 年创建。它用于跟踪文件的变化，协调多人之间的工作。

### 主要特点

- **分布式**：每个开发者都拥有完整的代码仓库副本
- **快照流**：Git 存储的是文件快照而非差异
- **高效性**：大多数操作在本地完成，速度快
- **完整性**：使用 SHA-1 哈希确保数据完整性
- **分支管理**：轻量级的分支创建和合并

### 版本控制简史

```
本地版本控制系统 (RCS)
    ↓
集中式版本控制系统 (CVS, SVN, Perforce)
    ↓
分布式版本控制系统 (Git, Mercurial)
```

---

## 为什么使用 Git

### 个人开发者

- 📜 **历史记录**：查看文件的所有修改历史
- ⏪ **版本回退**：随时回到之前的任何版本
- 🔀 **实验尝试**：在新分支上尝试新想法，不影响主代码

### 团队协作

- 👥 **并行开发**：多人同时工作，互不干扰
- 🔄 **代码合并**：自动或手动合并不同的修改
- 💾 **备份保护**：多份副本，数据更安全
- 🎯 **分支策略**：清晰的开发流程管理

### 企业应用

- 🚀 **持续集成/部署**：与 CI/CD 工具无缝集成
- 📊 **代码审查**：通过 Pull Request 进行代码审核
- 🔐 **权限管理**：细粒度的访问控制
- 📈 **项目追踪**：关联问题跟踪和项目管理

---

## Git 核心概念

### 三个工作区

Git 有三个主要的工作区域：

```
┌─────────────┐     ┌─────────────┐      ┌─────────────┐
│   工作区     │────▶│   暂存区      │────▶ │   仓库      │
│ (Working)   │ add │   (Stage)   │commit│ (Repository)│
└─────────────┘     └─────────────┘      └─────────────┘
     ↑                                       │
     │────────────── checkout ───────────────┘
```

#### 1. 工作区（Working Directory）
- 你实际操作的文件区域
- 可以看到和编辑文件
- `git status` 查看状态

#### 2. 暂存区（Staging Area / Index）
- 准备提交的文件列表
- 临时存放下次提交的内容
- `git add` 将文件添加到暂存区
- `git diff --staged` 查看暂存区差异

#### 3. 仓库（Repository）
- Git 保存项目元数据和对象数据库的地方
- 提交后的历史记录
- `git commit` 将暂存区内容提交到仓库

### 文件状态

文件在 Git 中有四种状态：

```
未跟踪 (Untracked)
    │
    │ git add
    ↓
已修改 (Modified) ──▶ 已暂存 (Staged)
    │                     │
    │ git add             │ git commit
    └─────────────────────┘
              │
              ↓
         已提交 (Committed)
```

#### 1. 未跟踪（Untracked）
- 新创建的文件
- Git 不会管理这些文件

#### 2. 已修改（Modified）
- 文件被修改但未添加到暂存区
- 需要使用 `git add` 添加

#### 3. 已暂存（Staged）
- 文件已添加到暂存区
- 等待被提交

#### 4. 已提交（Committed）
- 文件已保存到 Git 仓库
- 成为历史记录的一部分

### Git 对象模型

Git 存储四种对象：

1. **Blob（块）**：文件内容
2. **Tree（树）**：目录结构
3. **Commit（提交）**：提交快照
4. **Tag（标签）**：特定提交的引用

```
commit ──▶ tree ──▶ blob
         │        └──▶ blob
         └──▶ tree ───▶ blob
```

---

## Git 基本操作

### 初始化仓库

#### 创建新仓库

```bash
# 在当前目录初始化 Git 仓库
git init 

# 在指定目录创建仓库
git init my-project

# 初始化裸仓库（用于服务器）
git init --bare
```

#### 克隆现有仓库

```bash
# 克隆仓库
git clone https://github.com/user/repo.git

# 克隆到指定目录
git clone https://github.com/user/repo.git my-folder

# 克隆特定分支
git clone -b develop https://github.com/user/repo.git
```

### 配置 Git

#### 查看配置

```bash
# 查看所有配置
git config --list

# 查看特定配置
git config user.name
```

#### 设置用户信息

```bash
# 全局配置（所有仓库）
git config --global user.name "Your Name"
git config --global user.email "your.email@example.com"

# 局部配置（当前仓库）
git config user.name "Your Name"
git config user.email "your.email@example.com"

# 设置默认分支名称
git config --global init.defaultBranch main
```

#### 常用配置

```bash
# 设置默认编辑器
git config --global core.editor vim

# 设置差异工具
git config --global merge.tool vimdiff

# 设置别名
git config --global alias.co checkout
git config --global alias.br branch
git config --global alias.ci commit
git config --global alias.st status

# 颜色输出
git config --global color.ui true
```

### 查看状态

```bash
# 查看当前状态
git status

# 简洁输出
git status -s

# 显示分支信息
git status -sb
```

### 添加文件到暂存区

```bash
# 添加指定文件
git add file.txt

# 添加所有文件
git add .

# 添加所有修改的文件（不包括删除的文件）
git add -u

# 添加所有变化（包括新文件、修改、删除）
git add -A

# 交互式添加
git add -i
```

### 提交更改

```bash
# 提交暂存区的更改
git commit -m "提交说明"

# 添加并提交（跳过 git add）
git commit -am "提交说明"

# 修改最后一次提交
git commit --amend

# 修改最后一次提交说明
git commit --amend -m "新的提交说明"

# 空提交（用于触发 CI）
git commit --allow-empty -m "触发构建"
```

#### 提交信息规范

```bash
# 格式：<type>(<scope>): <subject>

# 类型（type）
feat: 新功能
fix: 修复 bug
docs: 文档更新
style: 代码格式（不影响代码运行的变动）
refactor: 重构（既不是新增功能，也不是修改 bug 的代码变动）
test: 增加测试
chore: 构建过程或辅助工具的变动

# 示例
git commit -m "feat(auth): 添加用户登录功能"
git commit -m "fix(api): 修复用户列表接口错误"
git commit -m "docs(readme): 更新安装说明"
```

### 查看差异

```bash
# 查看工作区与暂存区的差异
git diff

# 查看暂存区与最后一次提交的差异
git diff --staged
git diff --cached

# 查看工作区与最后一次提交的差异
git diff HEAD

# 查看两次提交之间的差异
git diff commit1 commit2

# 查看指定文件的差异
git diff file.txt

# 仅显示修改的文件名
git diff --name-only
```

### 查看历史

```bash
# 查看提交历史
git log

# 简洁显示
git log --oneline

# 显示分支图
git log --graph --oneline --all

# 显示最近 N 条记录
git log -n 5

# 显示指定文件的修改历史
git log file.txt

# 查看某次提交的详细内容
git show <commit-hash>

# 查看某次提交修改的文件
git show --name-only <commit-hash>
```

#### 格式化输出

```bash
# 自定义格式
git log --pretty=format:"%h - %an, %ar : %s"

# 常用占位符
# %H: 完整的 commit hash
# %h: 简短的 commit hash
# %an: 作者名字
# %ae: 作者邮箱
# %ad: 作者日期
# %ar: 相对时间
# %s: 提交说明

# 示例输出
git log --pretty=format:"%h | %ar | %s" --graph
```

### 撤销操作

#### 撤销工作区修改

```bash
# 撤销指定文件的修改
git restore file.txt
# 或旧版
git checkout -- file.txt

# 撤销所有修改
git restore .
```

#### 撤销暂存区

```bash
# 取消暂存指定文件
git restore --staged file.txt
# 或旧版
git reset HEAD file.txt

# 取消所有暂存
git restore --staged .
```

#### 回退提交

```bash
# 软重置（保留工作区和暂存区）
git reset --soft HEAD~1

# 混合重置（保留工作区，清空暂存区）
git reset --mixed HEAD~1
git reset HEAD~1  # 默认

# 硬重置（清空工作区和暂存区）
git reset --hard HEAD~1

# 回退到指定提交
git reset --hard <commit-hash>
```

### 删除文件

```bash
# 删除文件并暂存
git rm file.txt

# 仅从 Git 中删除，保留本地文件
git rm --cached file.txt

# 删除文件夹
git rm -r folder/
```

### 移动/重命名文件

```bash
# 重命名文件
git mv old.txt new.txt

# 移动文件
git mv file.txt folder/
```

---

## 分支管理基础

### 分支概念

Git 的分支非常轻量，它只是指向某个 commit 的可移动指针。

```
master (branch)
    │
    ▼
C1 ──▶ C2 ──▶ C3
```

创建新分支：

```
master     testing
  │           │
  ▼           ▼
C1 ──▶ C2 ──▶ C3
```

### 分支操作

#### 查看分支

```bash
# 查看本地分支
git branch

# 查看远程分支
git branch -r

# 查看所有分支
git branch -a

# 查看分支详细信息
git branch -v

# 查看分支关系图
git branch --merged
git branch --no-merged
```

#### 创建分支

```bash
# 创建新分支
git branch testing

# 创建并切换到新分支
git checkout -b testing
# 或使用新命令
git switch -c testing

# 从指定提交创建分支
git branch testing <commit-hash>
```

#### 切换分支

```bash
# 切换到已有分支
git checkout testing
# 或使用新命令
git switch testing

# 切换到上一个分支
git checkout -
```

#### 删除分支

```bash
# 删除已合并的分支
git branch -d testing

# 强制删除分支
git branch -D testing

# 删除远程分支
git push origin --delete testing
```

#### 重命名分支

```bash
# 重命名当前分支
git branch -m new-name

# 重命名指定分支
git branch -m old-name new-name
```

### 合并分支

```bash
# 合并指定分支到当前分支
git merge testing

# 合并时不自动提交
git merge --no-commit testing

# 使用 Squash 合并（将所有提交压缩为一个）
git merge --squash testing

# 取消合并
git merge --abort
```

#### 合并类型

1. **快进合并（Fast-forward）**
```
master    testing
  │          │
  ▼          ▼
  C1   ─▶    C2

合并后：
master, testing
       │
       ▼
C1 ──▶ C2
```

2. **三方合并（3-way Merge）**
```
master    testing
  │          │
  ▼          ▼
C1 ──▶ C2    C3
      │      │
      └──▶   C4

合并后：
master, testing
      │
      ▼
C1 ──▶ C2 ──▶ C3
      │      │
      └──▶ C4
```

### 变基（Rebase）

```bash
# 将当前分支变基到 master
git rebase master

# 交互式变基
git rebase -i HEAD~3

# 继续 rebase
git rebase --continue

# 跳过当前提交
git rebase --skip

# 放弃 rebase
git rebase --abort
```

#### Merge vs Rebase

| 特性 | Merge | Rebase |
|------|-------|--------|
| 历史记录 | 保留完整历史 | 线性历史 |
| 分支可见 | 是 | 否 |
| 冲突处理 | 一次性解决 | 逐个解决 |
| 使用场景 | 保留分支上下文 | 保持历史整洁 |

### Squash（压缩提交）

Squash 的作用是将多个提交合并为一个，使提交历史更加简洁清晰。Git 中有两种常见的 squash 方式。

#### 方式一：Squash Merge

将目标分支的所有提交压缩为一个，合并到当前分支。不会产生合并提交，历史保持线性。

```bash
# 切换到目标分支（如 main）
git switch main

# 将 feature 分支的所有提交压缩为一个
git merge --squash feature

# 手动提交（此时提交还在暂存区，需要 commit 完成合并）
git commit -m "feat: 添加用户登录功能"
```

效果示意：

```
合并前：
main:    C1 ──▶ C2
feature: C1 ──▶ C2 ──▶ C3 ──▶ C4 ──▶ C5

合并后：
main:    C1 ──▶ C2 ──▶ C6（C3+C4+C5 的合并）
```

#### 方式二：Interactive Rebase Squash

通过交互式变基，选择性地将多个提交压缩为一个。

```bash
# 编辑最近 3 次提交
git rebase -i HEAD~3
```

执行后会打开编辑器，显示类似内容：

```
pick e3a1b35 feat: 添加登录页面
pick 7ac9a02 fix: 修复表单验证
pick 4b2d8c1 docs: 更新注释

# 将 pick 改为 squash（或简写 s）来合并提交
pick e3a1b35 feat: 添加登录页面
squash 7ac9a02 fix: 修复表单验证
squash 4b2d8c1 docs: 更新注释
```

保存后会提示编辑合并后的提交信息。

#### 两种方式对比

| 特性 | Squash Merge | Interactive Rebase Squash |
|------|-------------|---------------------------|
| 操作对象 | 整个分支的提交 | 选定的若干提交 |
| 是否产生合并提交 | 否 | 否 |
| 原分支是否保留 | 是 | 原提交被重写 |
| 适用场景 | 合并功能分支 | 整理本地提交历史 |

#### 使用建议

- **功能分支合入主分支时**：使用 Squash Merge，保持主分支历史简洁
- **本地提交整理时**：使用 Interactive Rebase Squash，将零碎的提交合并为有意义的原子提交
- **已推送到远程的提交**：避免使用 rebase squash，因为会改写历史，需要 `git push --force`

---

## 远程仓库

### 查看远程仓库

```bash
# 查看远程仓库
git remote

# 查看详细信息
git remote -v

# 查看指定远程仓库信息
git remote show origin
```

### 添加远程仓库

```bash
# 添加远程仓库
git remote add origin https://github.com/user/repo.git

# 添加多个远程仓库
git remote add upstream https://github.com/original/repo.git
```

### 删除和重命名

```bash
# 删除远程仓库
git remote remove origin

# 重命名远程仓库
git remote rename origin new-origin
```

### 拉取和推送

```bash
# 拉取远程更改并自动合并
git pull

# 拉取但不合并
git fetch

# 拉取指定分支
git pull origin main

# 推送到远程
git push

# 推送指定分支
git push origin main

# 首次推送并设置上游
git push -u origin main

# 推送所有分支
git push --all

# 推送标签
git push --tags
```

---

## .gitignore 文件

### 作用

`.gitignore` 文件告诉 Git 哪些文件不需要被跟踪。

### 示例

```gitignore
# 忽略所有 .a 文件
*.a

# 但不忽略 lib.a
!lib.a

# 忽略 build 目录
build/

# 忽略 .txt 文件
*.txt

# 只忽略当前目录的 todo.txt
/todo.txt

# 忽略所有目录下的 doc 目录
**/doc/

# 常见模式
node_modules/
.DS_Store
*.log
.env
dist/
```

### 常见模板

```gitignore
# 操作系统文件
.DS_Store
Thumbs.db

# 编辑器
.vscode/
.idea/
*.swp
*.swo

# 依赖
node_modules/
vendor/

# 构建产物
dist/
build/
*.min.js

# 环境变量
.env
.env.local

# 日志
*.log
logs/

# 临时文件
*.tmp
*.temp
```

---

## 常见问题解答

### Q1: 如何查看 Git 配置？

```bash
# 查看所有配置
git config --list

# 查看特定级别配置
git config --list --global
git config --list --local
```

### Q2: 如何清空暂存区？

```bash
# 清空暂存区但保留工作区
git reset HEAD

# 清空暂存区和工作区
git reset --hard HEAD
```

### Q3: 如何找回丢失的提交？

```bash
# 查看所有操作记录
git reflog

# 恢复到指定提交
git reset --hard <commit-hash>
```

### Q4: 如何查看某行代码是谁写的？

```bash
# 查看文件每一行的修改信息
git blame file.txt

# 查看特定行
git blame -L 10,20 file.txt
```

### Q5: 如何处理合并冲突？

```bash
# 1. 查看冲突文件
git status

# 2. 手动编辑文件解决冲突

# 3. 标记为已解决
git add conflicted-file.txt

# 4. 完成合并
git commit
```

### Q6: 如何撤销已经推送的提交？

```bash
# 创建新的提交来撤销（推荐）
git revert <commit-hash>
git push

# 或强制推送（危险）
git reset --hard <commit-hash>
git push --force
```

### Q7: 如何查看提交历史中的文件内容？

```bash
# 查看某次提交时的文件内容
git show <commit-hash>:file.txt

# 恢复某次提交的文件到工作区
git checkout <commit-hash> -- file.txt
```

### Q8: 如何修改提交信息？

```bash
# 修改最后一次提交信息
git commit --amend -m "新的提交信息"

# 修改历史提交信息（交互式）
git rebase -i HEAD~n
# 将 pick 改为 edit，修改后：
git commit --amend
git rebase --continue
```

### Q9: 如何创建标签？

```bash
# 创建轻量标签
git tag v1.0.0

# 创建附注标签
git tag -a v1.0.0 -m "版本 1.0.0"

# 查看标签
git tag

# 推送标签
git push origin v1.0.0
git push --tags
```

### Q10: 如何存储临时工作？

```bash
# 保存当前工作
git stash

# 保存并添加说明
git stash save "临时保存"

# 查看存储列表
git stash list

# 恢复最近的存储
git stash pop

# 恢复但不删除
git stash apply

# 删除存储
git stash drop
git stash clear
```

---

## 练习建议

### 初级练习

1. 创建一个新仓库
2. 添加几个文件并提交
3. 修改文件并查看差异
4. 创建分支并合并

### 中级练习

1. 模拟多人协作场景
2. 处理合并冲突
3. 使用 rebase 整理历史
4. 使用 stash 管理临时工作

### 高级练习

1. 使用交互式 rebase
2. 执行 cherry-pick 操作
3. 使用 bisect 查找问题
4. 编写 Git hooks

---

## 下一步

完成本教程后，你可以：

1. 阅读 [02-命令速查表.md](./02-命令速查表.md) - 熟悉更多 Git 命令
2. 学习 [03-工作流程.md](./03-工作流程.md) - 了解团队协作流程
3. 练习 [04-实战案例.md](./04-实战案例.md) - 实战应用场景

---

**📚 继续学习：[02-命令速查表.md](./02-命令速查表.md)**
