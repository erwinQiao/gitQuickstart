# Git 工作流程

## 目录
- [工作流程概述](#工作流程概述)
- [Git Flow 工作流](#git-flow-工作流)
- [GitHub Flow 工作流](#github-flow-工作流)
- [GitLab Flow 工作流](#gitlab-flow-工作流)
- [分支策略](#分支策略)
- [Code Review 流程](#code-review-流程)
- [冲突解决](#冲突解决)
- [最佳实践](#最佳实践)

---

## 工作流程概述

### 什么是工作流程

Git 工作流程是一套使用 Git 进行版本控制的规范和最佳实践，它定义了：

- 🌿 **分支创建策略** - 何时创建分支
- 🔀 **合并规则** - 如何合并代码
- 📝 **提交规范** - 提交信息的格式
- 👥 **协作方式** - 团队如何协作
- 🚀 **发布流程** - 如何发布版本

### 选择合适的流程

| 工作流 | 适用场景 | 团队规模 | 复杂度 |
|--------|----------|----------|--------|
| **Centralized** | 小型项目、持续部署 | 2-5人 | ⭐ |
| **GitHub Flow** | Web应用、持续部署 | 5-20人 | ⭐⭐ |
| **Git Flow** | 有明确版本发布周期 | 10+人 | ⭐⭐⭐ |
| **GitLab Flow** | 需要环境和部署追踪 | 20+人 | ⭐⭐⭐⭐ |

---

## Git Flow 工作流

### 核心概念

Git Flow 由 Vincent Driessen 提出，是一个严格的分支模型，适合有**计划发布周期**的项目。

### 分支结构

```
master (主分支)
    │
    ├── develop (开发分支)
    │         │
    │         ├── feature/* (功能分支)
    │         ├── release/* (发布分支)
    │         └── hotfix/* (修复分支)
    │
    └── tags (版本标签)
```

### 分支说明

#### 1. Master 分支

- **作用**：生产环境代码
- **特点**：
  - 始终保持稳定
  - 只接受来自 develop 和 hotfix 的合并
  - 每次合并打上版本标签

#### 2. Develop 分支

- **作用**：开发主分支
- **特点**：
  - 功能开发的集成分支
  - 功能完成后合并到此处
  - 准备发布时创建 release 分支

#### 3. Feature 分支

- **作用**：开发新功能
- **命名**：`feature/功能名`
- **来源**：从 develop 创建
- **去向**：合并回 develop

#### 4. Release 分支

- **作用**：发布准备
- **命名**：`release/x.x.x`
- **来源**：从 develop 创建
- **去向**：合并到 master 和 develop

#### 5. Hotfix 分支

- **作用**：紧急修复生产问题
- **命名**：`hotfix/修复名`
- **来源**：从 master 创建
- **去向**：合并到 master 和 develop

### Git Flow 流程图

```
时间线 →

feature           feature          feature
  │                 │                │
  ▼                 ▼                ▼
┌─────┐          ┌─────┐         ┌─────┐
│ f-1 │─────────▶│ f-2 │────────▶│ f-3 │
└─────┘          └─────┘         └─────┘
   │                │                │
   └────────────────┼────────────────┘
                    ▼
              ┌──────────┐
              │ develop  │◀────────────┐
              └──────────┘             │
                    │                  │
                    ▼                  │
              ┌──────────┐             │
              │ release  │             │
              └──────────┘             │
                    │                  │
        ┌───────────┴───────────┐      │
        ▼                       ▼      │
   ┌─────────┐            ┌─────────┐  │
   │ master  │            │ develop │  │
   └─────────┘            └─────────┘  │
        │                             │
        ▼                             │
   ┌─────────┐                        │
   │ hotfix  │────────────────────────┘
   └─────────┘
        │
        ▼
   ┌─────────┐
   │ master  │
   └─────────┘
```

### Git Flow 实战

#### 1. 开始新功能

```bash
# 切换到 develop 分支
git checkout develop

# 拉取最新代码
git pull origin develop

# 创建功能分支
git checkout -b feature/user-authentication

# 开发功能...

# 提交更改
git add .
git commit -m "feat(auth): 添加用户登录功能"

# 推送到远程
git push -u origin feature/user-authentication
```

#### 2. 完成功能开发

```bash
# 切换到 develop
git checkout develop
git pull origin develop

# 合并功能分支
git merge feature/user-authentication

# 删除功能分支
git branch -d feature/user-authentication

# 推送更改
git push origin develop

# 删除远程分支（可选）
git push origin --delete feature/user-authentication
```

#### 3. 准备发布

```bash
# 从 develop 创建 release 分支
git checkout develop
git checkout -b release/1.2.0

# 修复 bug、更新版本号...

# 提交更改
git commit -a -m "chore(release): 1.2.0 版本准备"

# 合并到 master
git checkout master
git merge release/1.2.0

# 打标签
git tag -a v1.2.0 -m "Release version 1.2.0"

# 合并回 develop
git checkout develop
git merge release/1.2.0

# 删除 release 分支
git branch -d release/1.2.0

# 推送
git push origin master
git push origin develop
git push --tags
```

#### 4. 紧急修复

```bash
# 从 master 创建 hotfix 分支
git checkout master
git checkout -b hotfix/critical-bug

# 修复问题...

# 提交更改
git commit -a -m "fix: 修复严重的安全漏洞"

# 合并到 master
git checkout master
git merge hotfix/critical-bug
git tag -a v1.2.1 -m "Hotfix version 1.2.1"

# 合并到 develop
git checkout develop
git merge hotfix/critical-bug

# 删除 hotfix 分支
git branch -d hotfix/critical-bug

# 推送
git push
git push --tags
```

### Git Flow 工具

```bash
# 安装 git-flow
# macOS
brew install git-flow

# Ubuntu/Debian
sudo apt-get install git-flow

# 初始化 git-flow
git flow init

# 开始新功能
git flow feature start feature-name

# 完成功能
git flow feature finish feature-name

# 开始发布
git flow release start 1.0.0

# 完成发布
git flow release finish 1.0.0

# 开始修复
git flow hotfix start hotfix-name

# 完成修复
git flow hotfix finish hotfix-name
```

### 优缺点

**优点**
- ✅ 清晰的分支结构
- ✅ 支持并行的开发和发布
- ✅ 适合有明确版本计划的项目
- ✅ 紧急修复流程完善

**缺点**
- ❌ 复杂度较高
- ❌ 持续部署场景下过于繁琐
- ❌ 需要团队严格遵循规范

---

## GitHub Flow 工作流

### 核心概念

GitHub Flow 是一个**简化**的工作流，适合**持续部署**的项目。

### 分支结构

```
main (主分支)
    │
    └── feature/* (功能分支)
```

### 流程图

```
1. 创建分支
   main ──▶ feature/new-feature

2. 提交更改
   feature/new-feature (开发中)

3. 创建 Pull Request
   feature/new-feature ──▶ PR

4. Code Review
   Review PR

5. 合并到 main
   feature/new-feature ──▶ main

6. 立即部署
   main ──▶ Production
```

### GitHub Flow 实战

#### 1. 创建功能分支

```bash
# 确保在 main 分支
git checkout main
git pull origin main

# 创建功能分支
git checkout -b feature/new-feature

# 或者使用约定式提交
git checkout -b feat/add-user-profile
```

#### 2. 开发并提交

```bash
# 进行开发...

# 频繁提交
git add .
git commit -m "feat: 添加用户个人资料页面"

# 推送到远程
git push -u origin feature/new-feature
```

#### 3. 创建 Pull Request

在 GitHub 网页上：
1. 点击 "Compare & pull request"
2. 填写 PR 描述
3. 添加 reviewers
4. 关联相关 Issue

#### 4. Code Review

```bash
# 根据反馈修改代码
git add .
git commit -m "fix: 修复 review 中发现的问题"
git push
```

#### 5. 合并 PR

在 GitHub 上：
1. 点击 "Merge pull request"
2. 使用 "Squash and merge" 保持历史整洁
3. 删除功能分支

#### 6. 部署

```bash
# 拉取最新代码
git checkout main
git pull origin main

# 部署到生产环境
# (CI/CD 自动触发)
```

### PR 模板

创建 `.github/pull_request_template.md`:

```markdown
## 变更说明
<!-- 描述本次 PR 的主要变更内容 -->

## 变更类型
- [ ] 新功能
- [ ] Bug 修复
- [ ] 性能优化
- [ ] 重构
- [ ] 文档更新
- [ ] 测试更新

## 测试
<!-- 说明如何测试这些变更 -->

## 截图（如适用）
<!-- 添加截图或 GIF -->

## 关联 Issue
Closes #(issue number)

## 检查清单
- [ ] 代码符合项目规范
- [ ] 已添加必要的测试
- [ ] 文档已更新
- [ ] 所有测试通过
```

### 优缺点

**优点**
- ✅ 流程简单，易于理解
- ✅ 适合持续部署
- ✅ PR 提供良好的审查机制
- ✅ 历史记录清晰

**缺点**
- ❌ 不适合有长期版本支持的项目
- ❌ 需要完善的 CI/CD 支持
- ❌ main 分支可能不稳定

---

## GitLab Flow 工作流

### 核心概念

GitLab Flow 结合了 Git Flow 和 GitHub Flow 的优点，强调**环境分支**和**追踪发布**。

### 分支结构

```
main (主分支)
    │
    ├── staging (预生产)
    │         │
    └── production (生产)

从 main 创建 MR (Merge Request)
```

### 环境分支流程

```
1. 功能开发
   feature/* ──▶ main (通过 MR)

2. 部署到预生产
   main ──▶ staging (通过 MR)

3. 部署到生产
   staging ──▶ production (通过 MR)
```

### GitLab Flow 实战

#### 1. 开发功能

```bash
# 创建功能分支
git checkout -b feature/payment-gateway

# 开发并提交
git add .
git commit -m "feat: 添加支付网关集成"

# 推送并创建 MR
git push -u origin feature/payment-gateway
```

#### 2. 合并到 main

```bash
# MR 通过后合并到 main
git checkout main
git pull origin main
```

#### 3. 部署到 staging

```bash
# 创建 MR 到 staging
git checkout -b deploy-to-staging
git checkout staging
git merge deploy-to-staging

# 或者在 GitLab 上通过 MR 进行
```

#### 4. 部署到 production

```bash
# 同样的流程，创建 MR 到 production
git checkout production
git merge staging
git push origin production
```

### 版本标签

```bash
# 在 production 分支打标签
git checkout production
git tag -a v1.2.0 -m "Production release 1.2.0"
git push origin v1.2.0
```

### 优缺点

**优点**
- ✅ 支持多环境部署
- ✅ 可追踪每个环境的发布历史
- ✅ 结合了 CI/CD 的优势
- ✅ 灵活性高

**缺点**
- ❌ 配置相对复杂
- ❌ 需要良好的 CI/CD 支持
- ❌ 学习曲线较陡

---

## 分支策略

### 分支命名规范

```bash
# 功能分支
feature/功能名称
feat/功能名称

# 修复分支
fix/问题描述
bugfix/问题描述
hotfix/紧急修复

# 发布分支
release/x.x.x

# 实验性分支
experiment/实验名称

# 个人分支
username/功能名称
```

### 分支生命周期

```
短期分支（1-3天）
├── feature/*
├── fix/*
└── experiment/*

中期分支（1-2周）
├── release/*
└── develop (部分流程)

长期分支
├── main/master
└── staging/production
```

### 分支保护规则

```bash
# GitHub Settings → Branches → Branch Protection

# 主要分支保护
- main/master
  - Require pull request reviews (1+)
  - Require status checks to pass
  - Require branches to be up to date
  - Restrict who can push
  - Do not allow bypassing

# 辅助分支保护
- develop
  - Require reviews
  - Require CI checks
```

---

## Code Review 流程

### Code Review 的重要性

- 🐛 **发现 Bug**：多人审查减少错误
- 📚 **知识共享**：团队成员相互学习
- ✨ **代码质量**：保持代码风格一致
- 🎯 **最佳实践**：推广良好的编程模式

### Pull Request 最佳实践

#### 1. PR 大小

```
理想的 PR：
- 200-400 行代码变更
- 1-3 天的工作量
- 单一功能或修复

避免：
- 超过 1000 行
- 包含多个不相关功能
- 大规模重构
```

#### 2. PR 描述

```markdown
## 📝 变更说明
简洁描述 PR 的目的和内容

## 🎯 变更原因
解释为什么需要这个变更

## ✅ 变更内容
- 添加了 XXX 功能
- 修复了 XXX 问题
- 重构了 XXX 模块

## 🧪 测试说明
1. 运行测试：`npm test`
2. 手动测试步骤：
   - 访问 /page
   - 点击按钮
   - 验证结果

## 📸 截图/录屏
（如果是 UI 变更）

## 🔗 相关链接
- Issue: #123
- 设计稿: [link]
- 文档: [link]

## ⚠️ 注意事项
- 需要更新 API 文档
- 需要数据库迁移
```

#### 3. 审查清单

**功能完整性**
- [ ] 实现了所有需求
- [ ] 边界情况已处理
- [ ] 错误处理完善

**代码质量**
- [ ] 代码风格一致
- [ ] 命名清晰易懂
- [ ] 逻辑简洁高效
- [ ] 没有重复代码

**测试**
- [ ] 单元测试覆盖
- [ ] 集成测试通过
- [ ] 手动测试完成

**文档**
- [ ] 代码注释充分
- [ ] API 文档更新
- [ ] README 更新

### Review 流程

#### 1. 提交审查

```bash
# 标记需要审查的人员
@username 请审查这个 PR

# 请求特定类型的审查
@username 请关注性能方面
@username 请检查安全相关代码
```

#### 2. 审查反馈

```markdown
# 建设性的反馈

❌ 避免：
- "这代码很差"
- "重写这个函数"

✅ 推荐：
- "建议使用 XXX 替代，因为..."
- "这里可以简化为："
- "考虑提取为独立函数，提高可读性"
```

#### 3. 修改和重新审查

```bash
# 根据反馈修改
git checkout feature/branch
git add .
git commit -m "fix: 修复 review 反馈的问题"
git push

# PR 自动更新，reviewers 可以继续审查
```

### 审查工具

#### GitHub Review Features

```markdown
# 行内评论
// 可以在特定行添加评论

# 批准和请求修改
- Approve ✅
- Request Changes 🔄
- Comment 💬

# 提交审查
1. Files changed
2. 添加行内评论
3. 提交整体审查
```

---

## 冲突解决

### 冲突产生原因

```
场景 1：两个人修改同一文件的同一行
A: console.log("hello")
B: console.log("world")

场景 2：一个人修改，另一个人删除
A: 修改 function.js
B: 删除 function.js

场景 3：两个人修改同一文件的不同行
(通常可以自动合并)
```

### 冲突标记

```bash
# Git 冲突标记
<<<<<<< HEAD
当前分支的代码
=======
合并分支的代码
>>>>>>> feature-branch
```

### 解决冲突步骤

#### 1. 发现冲突

```bash
# 合并时发现冲突
git merge feature-branch
# Auto-merging file.txt
# CONFLICT (content): Merge conflict in file.txt

# 拉取时发现冲突
git pull origin main
# CONFLICT (content): Merge conflict in file.txt
```

#### 2. 查看冲突

```bash
# 查看冲突文件
git status
# both modified:   file.txt

# 查看冲突内容
cat file.txt
```

#### 3. 解决冲突

```bash
# 方法 1：手动编辑
vim file.txt

# 保留需要的代码，删除冲突标记
<<<<<<< HEAD
console.log("hello")
=======
console.log("world")
>>>>>>> feature-branch

# 改为：
console.log("hello world")

# 方法 2：使用工具
git mergetool
```

#### 4. 标记解决

```bash
# 添加解决后的文件
git add file.txt

# 继续合并
git commit
```

### 冲突解决策略

#### 接受当前分支

```bash
# 接受当前分支的所有更改
git checkout --ours file.txt
git add file.txt
```

#### 接受合并分支

```bash
# 接受合并分支的所有更改
git checkout --theirs file.txt
git add file.txt
```

#### 部分接受

```bash
# 手动选择需要的部分
# 编辑文件，选择需要的代码
git add file.txt
```

### 中止操作

```bash
# 中止合并
git merge --abort

# 中止 rebase
git rebase --abort

# 中止 cherry-pick
git cherry-pick --abort
```

---

## 最佳实践

### 提交规范

#### Conventional Commits

```bash
# 格式
<type>(<scope>): <subject>

<body>

<footer>

# 示例
feat(auth): add user login feature

Implement OAuth2 authentication with Google and GitHub.

- Add login button to header
- Create auth service module
- Add user session management

Closes #123
```

#### Type 类型

```bash
feat:     新功能
fix:      Bug 修复
docs:     文档变更
style:    代码格式（不影响功能）
refactor: 重构
perf:     性能优化
test:     测试相关
chore:    构建/工具变更
ci:       CI 配置
revert:   回退提交
```

### 分支管理

#### 小而频繁的提交

```bash
# ✅ 好的实践
git add auth.js
git commit -m "feat(auth): add login function"

git add auth.test.js
git commit -m "test(auth): add login tests"

# ❌ 不好的实践
git add .
git commit -m "lots of changes"
```

#### 定期同步

```bash
# 定期从主分支同步
git checkout feature-branch
git fetch origin main
git rebase origin/main
```

### 安全实践

#### 敏感信息

```bash
# 不要提交
- 密码和密钥
- API tokens
- 用户数据
- .env 文件

# 使用 .gitignore
.env
*.key
credentials.json
```

#### 密钥管理

```bash
# 使用环境变量
export API_KEY="xxx"

# 使用密钥管理工具
- git-crypt (加密)
- git-secrets (防止提交密钥)
- truffleHog (扫描密钥)
```

### 性能优化

#### 仓库大小

```bash
# 查看仓库大小
git count-objects -vH

# 清理大文件
git gc --aggressive --prune=now

# 使用 Git LFS (Large File Storage)
git lfs track "*.psd"
git lfs track "*.zip"
```

#### 拉取优化

```bash
# 浅克隆（只获取最近历史）
git clone --depth 1 <url>

# 只拉取需要的分支
git clone -b main --single-branch <url>

# 稀疏检出（只检出部分目录）
git clone --sparse <url>
git sparse-checkout set src/
```

### 团队协作

#### 约定式提交

```bash
# 团队统一提交格式
git commit -m "feat(api): add user endpoint"
git commit -m "fix(ui): resolve navigation bug"
```

#### 代码审查

```bash
# 每个代码至少一个审查者
# 主要代码需要两个审查者

# 审查要点
1. 功能正确性
2. 代码质量
3. 性能影响
4. 安全问题
5. 测试覆盖
```

#### CI/CD 集成

```yaml
# .github/workflows/ci.yml
name: CI
on: [push, pull_request]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: npm install
      - run: npm test
      - run: npm run lint
```

---

## 工作流比较

### 选择指南

```
小团队 + 持续部署
→ GitHub Flow

大团队 + 版本发布
→ Git Flow

需要多环境 + 追踪发布
→ GitLab Flow

简单项目 + 快速迭代
→ 简化 Flow（main + feature）
```

### 对比表

| 特性 | Git Flow | GitHub Flow | GitLab Flow |
|------|----------|-------------|-------------|
| 复杂度 | 高 | 低 | 中 |
| 发布周期 | 计划发布 | 持续部署 | 灵活 |
| 分支数量 | 5+ | 2 | 3-4 |
| 适用项目 | 企业应用 | Web应用 | 企业/Web |
| CI/CD要求 | 中 | 高 | 高 |

---

## 总结

### 关键要点

1. **选择合适的流程**
   - 根据团队规模和项目特点选择

2. **遵守分支规范**
   - 统一命名
   - 及时清理

3. **重视 Code Review**
   - 提高代码质量
   - 促进团队学习

4. **处理冲突**
   - 保持冷静
   - 沟通优先

5. **持续改进**
   - 定期回顾流程
   - 优化工作方式

### 学习建议

1. 从简单的流程开始
2. 逐步建立规范
3. 使用工具自动化
4. 培养良好习惯

---

**📚 继续学习：[04-实战案例.md](./04-实战案例.md)**
